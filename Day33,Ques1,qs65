//Search in sorted array at the appropriate position.
#include <stdio.h>

int main() {
    int n, key, pos = -1;

    printf("Enter size of sorted array: ");
    scanf("%d", &n);

    int arr[n];

    printf("Enter %d sorted elements:\n", n);
    for (int i = 0; i < n; i++) {
        scanf("%d", &arr[i]);
    }

    printf("Enter element to search appropriate position for: ");
    scanf("%d", &key);

    // Find position where

