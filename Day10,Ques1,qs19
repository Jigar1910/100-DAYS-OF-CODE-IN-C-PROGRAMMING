// Q19: Write a program to classify a triangle as Equilateral, Isosceles, or Scalene based on its side lengths.

#include <stdio.h>

int main() {
    float a,b,c;
    printf("Enter the sides of a,b,c:");
    scanf("%f%f%f",&a,&b,&c);
    if((a+b>c) && (a+c>b) && (b+c>a)){
        if(a==b && b==c){
            printf("Equilateral triangle:");
        }
        else if(a==b || b==c || a==c){
            printf("Isosceles triangle");
        }
        else{
            printf("Invalid triangle - sides do not satisfy triangle property");
        }
    }

    return 0;
}
