// Rotate an array to the right by k positions.
#include <stdio.h>

int main() {
    int n, k;

    printf("Enter size of array: ");
    scanf("%d", &n);

    int arr[n], rotated[n];

    printf("Enter %d elements:\n", n);
    for (int i = 0; i < n; i++) {
        scanf("%d", &arr[i]);
    }

    printf("Enter value of k (right rotations): ");
    scanf("%d", &k);

    k = k % n;  // handle cases where k > n

    // Perform right rotation
    for (int i = 0; i < n; i++) {
        rotate

